from setuptools import setup, find_packages

setup(
    name="dircraft",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.8",
    description="Create project folder structures from path lists",
    author="Tejas",
)
